You are continuing work in `c:\Users\philip\sauce\virtual-webcam\v2`.

Important workspace rules:
- Read `AGENTS.md` first.
- Always use the caveman skill unless I say otherwise.
- Before any `driver-project` edit, validate intended driver change against PDFs under `pdfs/`: read TOC first, then relevant section, and record/cite section in work log.
- All `.ps1` files must stay in `scripts/`.
- Driver tests normally run in VM `driver-test`; credentials are in `.env`.
- If the same error happens twice, research online.
- Keep test artifacts in `test-reports`; `output` is only staged binaries/package files.
- Do not touch ignored `wiki/`.

Current state:
- Build passed with `.\scripts\build-all.ps1`.
- Updated driver to advertise aspect-ratio formats:
  - 16:9 `1280x720`
  - 4:3 `640x480`
  - 9:16 `720x1280`
  - 3:4 `480x640`
- Updated driver frame property handling so uploads are sized from active stream geometry, not fixed 1280x720.
- Updated user-mode `DriverBridge` to query active driver geometry before scaling/uploading frames.
- Updated proof script to prefer YUY2, then NV12, then RGB32/Bgra8.
- Docs/todo were updated.
- Cleanup removed regenerated build work dirs, kept `output` and `test-reports`.

Important reports:
- Driver PDF validation/work log:
  `test-reports/driver-format-validation-20260505.md`
- Build/install in `driver-test`:
  `test-reports/driver-test-reinstall-20260505-150429`
- Four-format VM smoke passed:
  `test-reports/driver-test-format-proof-20260505-150550`
  Passed 16:9, 4:3, 9:16, 3:4 with live non-black frames.

vHLK status:
- vHLK VM: `vhlk`
- Target VM/machine: `driver-test`, HLK machine `WIN-KQ09DIBH3C6`
- HLK project: `VirtuaCam`
- Target: `Virtual Camera Driver`, key `ROOT\AVSHWS\0000`, hardware ID `AVSHWS`
- Correct playlist used:
  `C:\Users\Administrator\Desktop\Compat Playlists\HLK Version 2004 CompatPlaylist x86 x64 ARM64.xml`
  Reason: controller is HLK 10.1.19041.19041; 22H2 playlist selected zero tests previously.
- Clean vHLK run was queued at:
  `test-reports\vhlk-playlist-20260505-151314`
- Queue result:
  - Old results cancelled/deleted
  - 113 tests loaded
  - 0 missing GUIDs
  - 113 queued
- Problem: after 15 minutes, all tests stayed `InQueue,InQueue`; none started:
  `test-reports\vhlk-playlist-20260505-151314\monitor-15min.json`

Next steps:
1. Diagnose why vHLK queue is stuck with 113 tests `InQueue,InQueue`.
   - Check HLK machine status/pool in `vhlk`.
   - Confirm `driver-test` `HLKSvc` is running and heartbeating to controller.
   - Restart `HLKSvc` on `driver-test` if needed.
   - Check controller/job scheduler services on `vhlk`.
   - Check whether machine is still `Running` or blocked/not-ready in ObjectModel.
2. Once queue starts, monitor vHLK until either:
   - all 113 playlist tests pass, or
   - first failure occurs.
3. If failure occurs:
   - collect failed test logs under `test-reports\vhlk-playlist-20260505-151314`
   - summarize failed test name, command line, HRESULT/log snippet
   - fix only after validating driver changes against PDFs if touching `driver-project`.
4. If all pass:
   - update `todo.md`
   - update relevant docs if needed
   - final summary with paths to smoke + vHLK evidence.

Useful commands/patterns:
- Load Hyper-V helpers:
  `. .\scripts\hyperv-common.ps1`
- Read `.env` for VM credentials.
- Use PowerShell Direct via `Wait-HvPowerShellDirect`.
- vHLK ObjectModel assembly lives under:
  `C:\Program Files (x86)\Windows Kits\10\Hardware Lab Kit`
- To query project:
  load ObjectModel DLLs, create:
  `Microsoft.Windows.Kits.Hardware.ObjectModel.DBConnection.DatabaseProjectManager($env:COMPUTERNAME)`
  then:
  `$pm.GetProject('VirtuaCam').GetTests()`

Goal:
Finalize vHLK playlist tests. All necessary items in selected playlist should pass.
